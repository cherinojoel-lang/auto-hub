🎯 **What:**
Added a test file for the `cn` utility function (`src/lib/utils.ts`) which combines `clsx` and `tailwind-merge`. This closes a testing gap for a heavily-used utility function in the project.

📊 **Coverage:**
The tests now verify:
- Merging standard tailwind classes
- Overriding classes with `tailwind-merge` correctly
- Handling conditional classes dynamically via `clsx`
- Dealing properly with arrays, falsy values, null, undefined, and mixed inputs
- Complex scenarios combining all of the above

✨ **Result:**
Increased test coverage and confidence when making future UI component refactoring, as the core class-name merging behavior is now thoroughly verified.
