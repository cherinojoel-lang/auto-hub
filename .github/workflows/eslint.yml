name: ESLint

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 0 * * 0"

jobs:
  eslint:
    name: Run eslint scanning
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci --ignore-scripts || npm install --ignore-scripts

      - name: Install ESLint
        run: |
          npm install eslint@9 jiti
          npm install @microsoft/eslint-formatter-sarif

      - name: Run ESLint
        run: npx eslint . --format @microsoft/eslint-formatter-sarif/eslint -o eslint-results.sarif
        continue-on-error: true

      - name: Upload ESLint scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v4
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
        with:
          sarif_file: eslint-results.sarif
